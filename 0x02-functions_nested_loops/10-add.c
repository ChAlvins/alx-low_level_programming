#include "main.h"

/**
 * add - two integers and returns the result
 * @a: integer one
 * @b: integer two
 * Return: results of addition
 */
int add(int a, int b)
{
	int c;

	c = (a + b);
	return (c);
}
