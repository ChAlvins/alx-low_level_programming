#include "main.h"

/**
 *  _abs - computing absolute value of an integer
 *@n: interger to be computed
 * Return: abs value of int
 *
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
